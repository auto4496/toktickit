# Dedicated PostgreSQL target used only by Vitest.
# The database name or schema must contain a distinct "test" marker and must
# not point to the same database/schema as DATABASE_URL.
TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/toktickit_test?schema=public"
