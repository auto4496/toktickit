# Lab 1 - AI Use and Reflection

I used OpenAI Codex in the Codex desktop application with a GPT-5 model. I used it to help plan the work, troubleshoot Git and configuration problems, review code, implement features, and verify the final result. I checked the suggested changes and command output before accepting them.

## Selected Key Prompts

The prompts below are selected examples from my Lab 1 work, edited for clarity and translated into English where necessary.

| Prompt name | Actual prompt text | My reflection |
|---|---|---|
| Resolve the Issue 2 merge conflict | Inspect the conflicts between `feature/2-health-check` and the latest `lab1-staging` in `client/src/App.tsx`, `client/vite.config.ts`, `server/package.json`, and `server/src/app.ts`. Explain which changes belong to each Issue, preserve all completed Issue 1 files, and keep only Issue 2 work in the final PR. Provide the safe Git commands, then verify the diff and conflict markers before committing. | Stating the base branch, affected files, and work that had to be preserved made the conflict resolution safer. It also gave me checks to run before pushing. |
| Review my partner's Issue 1 | Review the attached branch against Issue 1 only. Check the required project setup, Prisma initialization, tests, repository structure, README, `.gitignore`, and `.env.example`. Separate blocking findings from optional suggestions and do not treat Issue 2-4 features as missing. | Limiting the scope prevented unrelated features from becoming false review findings. |
| Recreate Issue 2 from staging | Recreate `feature/2-health-check` from the latest `lab1-staging` because the existing PR is based on an outdated commit. Preserve all Issue 1 files, including the database evidence, and keep the new branch limited to the health endpoint, its Supertest test, and the Check System UI states. Use the exact service value `TokTickIT API`, then run all tests, both builds, `git diff --check`, and a base-branch comparison before pushing. | Including the correct base, preserved evidence, exact response value, and verification commands produced a much cleaner repair. |
| Fix Vite environment loading | The repository documents `VITE_API_URL` in the root `.env`, but Vite runs from `client/` and does not load it. Configure Vite to load the documented environment file and derive the development proxy target from `VITE_API_URL` or the configured server port without hard-coding port 5000. Confirm that the default setup still works, a changed URL takes effect, and the client build and tests pass. | Describing both the configuration mismatch and expected behavior led to a focused fix instead of changing unrelated API code. |
| Implement the category model and seed | Starting from the latest `lab1-staging`, implement Issue 3 on `feature/3-category-seed`. Add a Prisma `Category` model with an auto-incrementing ID, unique name, and creation timestamp; create the migration; and seed Account and Access, Hardware, Software, and Network using an idempotent approach. Load the documented root `.env`, keep credentials out of Git, and do not add the Issue 4 endpoint or UI. Verify the migration, run the seed twice, and confirm four rows with four distinct names. | This prompt mapped directly to the model, security, scope, and repeatability criteria. The explicit database checks made the result easier to verify. |
| Diagnose the PostgreSQL failure | `prisma migrate status` returns P1001 for `localhost:5432`, while Prisma validation succeeds. Use the `.env` location, Docker container list, port mappings, and `pg_isready` result to determine whether PostgreSQL is stopped, mapped to another port, or using the wrong database. Do not change the schema or application code until the connection cause is confirmed. | Providing the exact error and the successful validation result helped separate a database-service problem from a schema problem. |
| Implement and test Issue 4 | Implement Issue 4 from the staging branch that already contains the approved Issue 3 migration and seed. Add `GET /api/categories` using Prisma, return only `id` and `name` in ascending ID order, handle database failures, and add Supertest coverage. Update React so Check System requests both endpoints and renders loading, Online, Offline, and useful error states using API-provided categories rather than hard-coded values. Add UI tests for the heading, loading-to-list behavior, and failure state; then run all tests, both builds, and browser success/failure checks. | This prompt covered the complete database-to-API-to-UI slice while keeping the feature within Issue 4. Automated and browser checks provided different kinds of evidence. |
| Perform the final Lab 1 audit | Review the completed repository against the Lab sheet before merging `lab1-staging` into `main`. Check all four acceptance-criteria lists, feature-branch and PR history, approvals in both review directions, the 6-10 selected AI prompts, test evidence, builds, migration and seed behavior, README setup steps, ignored secrets, required file structure, and the GitHub Project name, status order, and final Done column. Report every remaining blocker and do not merge until the audit is complete. | The final audit found documentation and Project-board details that code tests could not detect. Adding a clear stop condition prevented an early merge. |

## Reflection

My first prompts were often too short and depended too much on the previous conversation. I obtained better results when I included the current branch, target branch, exact Issue scope, files or values that had to be preserved, and the checks required before completion. I also learned to verify AI findings rather than accept them immediately. One review result came from a restricted environment and did not reproduce in a clean installation, so I corrected the review after checking the evidence. For the later Issues, I used clearer constraints and confirmed the work with Git diffs, tests, builds, database checks, and browser behavior.
